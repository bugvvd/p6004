import React from 'react';
import {renderWithContext} from '../../../jest/utils/renderWithContext';
import Root from '..';

describe('Root Navigator', () => {
  it.todo('should render login screen');
  it.todo("should render RegisterScreen on pressing 'REGISTER' button");
});
