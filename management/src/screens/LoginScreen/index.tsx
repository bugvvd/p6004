// core
import React from 'react';

// components
import {View, Text} from 'react-native';

// types
import { LoginScreenProps } from '../../../lib/types/screen';

export default function LoginScreen({navigation, route}: LoginScreenProps) {
  return (
    <View>
      <Text>LoginScreen</Text>
    </View>
  );
}
