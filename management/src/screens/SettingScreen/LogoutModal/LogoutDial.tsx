import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const LogoutDial = (): JSX.Element => {
  return <View></View>;
};

const styles = StyleSheet.create({
  container: {},
  actions: {
    flex: 1,
  },
});

export default LogoutDial;
